{
  "examples": [
    {
      "capability": "code_analysis",
      "input": {
        "code": "function add(a, b) { return a + b; }",
        "language": "javascript",
        "analysis_type": "quality"
      },
      "output": {
        "score": 85,
        "issues": [],
        "suggestions": ["Consider adding type annotations", "Add JSDoc comments"]
      }
    },
    {
      "capability": "automated_testing",
      "input": {
        "code": "class Calculator { add(a, b) { return a + b; } }",
        "language": "javascript",
        "test_type": "unit"
      },
      "output": {
        "tests": [
          "describe('Calculator', () => { test('adds 1 + 2 to equal 3', () => { expect(new Calculator().add(1, 2)).toBe(3); }); });"
        ],
        "coverage": 100
      }
    },
    {
      "capability": "security_audit",
      "input": {
        "code": "const query = `SELECT * FROM users WHERE id = ${userId}`;",
        "language": "javascript",
        "scan_type": "vulnerability"
      },
      "output": {
        "vulnerabilities": [
          {
            "type": "SQL Injection",
            "severity": "critical",
            "line": 1,
            "recommendation": "Use parameterized queries"
          }
        ]
      }
    },
    {
      "capability": "performance_optimization",
      "input": {
        "code": "for (let i = 0; i < array.length; i++) { console.log(array[i]); }",
        "language": "javascript"
      },
      "output": {
        "optimizations": [
          {
            "type": "iteration",
            "suggestion": "Use array.forEach() or for...of for better readability",
            "impact": "minor"
          }
        ]
      }
    },
    {
      "capability": "documentation_generation",
      "input": {
        "code": "function calculateTax(amount, rate) { return amount * rate; }",
        "language": "javascript",
        "doc_type": "jsdoc"
      },
      "output": {
        "documentation": "/**\n * Calculates tax based on amount and rate\n * @param {number} amount - The base amount\n * @param {number} rate - The tax rate (as decimal)\n * @returns {number} The calculated tax amount\n */"
      }
    }
  ]
}