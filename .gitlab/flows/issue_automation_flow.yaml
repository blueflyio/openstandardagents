image: alpine:latest
injectGatewayToken: true

commands:
  - echo "Starting issue automation flow"

steps:
  - id: issue_triage
    agent: bot-issue-worker
    with:
      project_id: "{{ event.project_id }}"
      issue_id: "{{ event.issue_id }}"
