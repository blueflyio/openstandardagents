@bluefly:registry=https://registry.npmjs.org/
# Auth token: GITLAB_REGISTRY_NPM_TOKEN (platform) or NPM_TOKEN (npmjs). In CI set in variables.
# Do NOT put token values here — they override user-level ~/.npmrc
//registry.npmjs.org/:_authToken=${GITLAB_REGISTRY_NPM_TOKEN}
