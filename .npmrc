@bluefly:registry=https://registry.npmjs.org/
# Auth token must be set via NPM_TOKEN environment variable
# In CI: export NPM_TOKEN=npm_xxxx
# The //registry line is populated automatically by `npm login` or CI env
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
