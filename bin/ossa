#!/usr/bin/env node

/**
 * OSSA CLI - Open Standards for Scalable Agents
 * Professional validation and management tool
 */

import { fileURLToPath } from 'url';
import { dirname, join } from 'path';

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

// Import the main validation script
import(join(__dirname, '../lib/tools/validation/validate-ossa.js')).catch(console.error);