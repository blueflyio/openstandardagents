# =============================================================================
# OSSA Website - GitLab Registry Secret
# =============================================================================
# This secret is created dynamically - DO NOT commit actual credentials!
# Create with:
#   kubectl create secret docker-registry gitlab-registry \
#     --namespace=ossa-website \
#     --docker-server=registry.gitlab.com \
#     --docker-username=oauth2 \
#     --docker-password="YOUR_GITLAB_TOKEN"
# =============================================================================
# This file exists as a placeholder for GitOps documentation.
# The actual secret should be created via CI or manually.
