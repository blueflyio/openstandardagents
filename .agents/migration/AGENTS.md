# version-migrator

Migrate OSSA manifests between versions with zero downtime

## Purpose

Migrate OSSA manifests between versions (v0.3.x → v0.4.x).
Handles breaking changes, deprecated fields, and new features.
Validates before and after migration.

capabilities:

## Usage

```bash
ossa run version-migrator --help
```

## Documentation

See `version-migrator.ossa.yaml` for complete manifest specification.

## Agent Type

OSSA v0.4.0 Strategic Production Agent

Generated from: `version-migrator.ossa.yaml`
