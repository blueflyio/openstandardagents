apiVersion: '@bluefly/ossa/v0.1.9'
kind: Agent
metadata:
  name: incident-response-coordinator
  uuid: project-incident-response-coordinator-orchestrator-v1
  type: orchestrator
  version: 1.0.0
  description: 'OSSA-compliant orchestrator agent: incident-response-coordinator'
spec:
  capabilities:
    - multi_agent_coordination
    - workflow_orchestration
    - resource_allocation
  dependencies:
    - '@ossa/core@0.1.9'
    - express@^4.18.0
  endpoints:
    health: /health
    execute: /execute
    status: /status
    metrics: /metrics
  integrations:
    workspace:
      local: .agents-workspace
      global: ../.agent-workspace
    registry:
      local: ../registry.yml
      global: ../.agent-workspace/registry.yml
  behaviors:
    primary: incident-response-coordinator.behavior.yml
    additional: []
  validation:
    schema: schemas/incident-response-coordinator.schema.json
    openapi: openapi.yml
