# OSSA Agent Registry - openstandardagents
# Auto-generated by: ossa agents sync
# Manual edits will be overwritten
version: "1.0"
agents:
  - name: compliance-checker
    manifest: compliance-checker/agent.ossa.yaml
    kind: specialist
    labels: { category: compliance, tier: production, criticality: critical }

  - name: docs-generator
    manifest: docs-generator/agent.ossa.yaml
    kind: worker
    labels: { category: documentation, tier: production }

  - name: integration-tester
    manifest: integration-tester/agent.ossa.yaml
    kind: worker
    labels: { category: integration, tier: production }

  - name: performance-optimizer
    manifest: performance-optimizer/agent.ossa.yaml
    kind: specialist
    labels: { category: optimization, tier: production }

  - name: platform-exporter
    manifest: platform-exporter/agent.ossa.yaml
    kind: worker
    labels: { category: export, tier: production, criticality: critical }

  - name: release-manager
    manifest: release-manager/agent.ossa.yaml
    kind: orchestrator
    labels: { category: release, tier: production, criticality: critical }

  - name: schema-validator
    manifest: schema-validator/agent.ossa.yaml
    kind: specialist
    labels: { category: validation, tier: production }

  - name: security-auditor
    manifest: security-auditor/agent.ossa.yaml
    kind: specialist
    labels: { category: security, tier: production, criticality: critical }

  - name: test-suite-generator
    manifest: test-suite-generator/agent.ossa.yaml
    kind: worker
    labels: { category: testing, tier: production }

  - name: version-migrator
    manifest: version-migrator/agent.ossa.yaml
    kind: worker
    labels: { category: migration, tier: production }
