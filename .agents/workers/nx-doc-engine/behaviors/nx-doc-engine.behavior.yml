apiVersion: '@bluefly/ossa/v0.1.9'
kind: AgentBehavior
metadata:
  name: nx-doc-engine-behavior
  agent: nx-doc-engine
  version: 1.0.0
spec:
  behavior_type: worker
  triggers:
    - type: api_request
      endpoint: /execute
      method: POST
    - type: event_driven
      events:
        - task.assigned
    - type: scheduled
      cron: 0 */6 * * *
  capabilities:
    - task_execution
    - data_processing
    - api_interaction
  quality_gates:
    - name: input_validation
      required: true
      schema: schemas/input/nx-doc-engine.input.json
    - name: output_verification
      required: true
      schema: schemas/output/nx-doc-engine.output.json
  artifacts:
    outputs:
      - nx-doc-engine_logs
      - nx-doc-engine_results
    storage:
      local: .agents-workspace/data/artifacts
      global: ../.agent-workspace/data/artifacts
