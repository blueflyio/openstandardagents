apiVersion: '@bluefly/ossa/v0.1.9'
kind: Agent
metadata:
  name: drupal-migration-specialist
  uuid: project-drupal-migration-specialist-worker-v1
  type: worker
  version: 1.0.0
  description: 'OSSA-compliant worker agent: drupal-migration-specialist'
spec:
  capabilities:
    - task_execution
    - data_processing
    - api_interaction
  dependencies:
    - '@ossa/core@0.1.9'
    - express@^4.18.0
  endpoints:
    health: /health
    execute: /execute
    status: /status
    metrics: /metrics
  integrations:
    workspace:
      local: .agents-workspace
      global: ../.agent-workspace
    registry:
      local: ../registry.yml
      global: ../.agent-workspace/registry.yml
  behaviors:
    primary: drupal-migration-specialist.behavior.yml
    additional: []
  validation:
    schema: schemas/drupal-migration-specialist.schema.json
    openapi: openapi.yml
