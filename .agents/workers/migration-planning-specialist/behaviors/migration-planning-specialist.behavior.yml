apiVersion: '@bluefly/ossa/v0.1.9'
kind: AgentBehavior
metadata:
  name: migration-planning-specialist-behavior
  agent: migration-planning-specialist
  version: 1.0.0
spec:
  behavior_type: worker
  triggers:
    - type: api_request
      endpoint: /execute
      method: POST
    - type: event_driven
      events:
        - task.assigned
    - type: scheduled
      cron: 0 */6 * * *
  capabilities:
    - task_execution
    - data_processing
    - api_interaction
  quality_gates:
    - name: input_validation
      required: true
      schema: schemas/input/migration-planning-specialist.input.json
    - name: output_verification
      required: true
      schema: schemas/output/migration-planning-specialist.output.json
  artifacts:
    outputs:
      - migration-planning-specialist_logs
      - migration-planning-specialist_results
    storage:
      local: .agents-workspace/data/artifacts
      global: ../.agent-workspace/data/artifacts
