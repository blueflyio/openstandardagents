apiVersion: '@bluefly/ossa/v0.1.9'
kind: Agent
metadata:
  name: disaster-recovery-planner
  uuid: project-disaster-recovery-planner-worker-v1
  type: worker
  version: 1.0.0
  description: 'OSSA-compliant worker agent: disaster-recovery-planner'
spec:
  capabilities:
    - task_execution
    - data_processing
    - api_interaction
  dependencies:
    - '@ossa/core@0.1.9'
    - express@^4.18.0
  endpoints:
    health: /health
    execute: /execute
    status: /status
    metrics: /metrics
  integrations:
    workspace:
      local: .agents-workspace
      global: ../.agent-workspace
    registry:
      local: ../registry.yml
      global: ../.agent-workspace/registry.yml
  behaviors:
    primary: disaster-recovery-planner.behavior.yml
    additional: []
  validation:
    schema: schemas/disaster-recovery-planner.schema.json
    openapi: openapi.yml
