openapi: 3.1.0
info:
  title: websocket-handler-expert Agent API
  version: 1.0.0
  description: OSSA-compliant API specification for websocket-handler-expert
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Agent is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: healthy
                  agent:
                    type: string
                    example: websocket-handler-expert
                  version:
                    type: string
                    example: 1.0.0
