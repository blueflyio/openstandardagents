// OSSA Bot version constants
// Synced with website lib/version.ts - auto-updated on releases

export const OSSA_VERSION = "0.2.9";
export const OSSA_VERSION_TAG = `v${OSSA_VERSION}`;
export const OSSA_API_VERSION = `ossa/v${OSSA_VERSION}`;
export const OSSA_SCHEMA_VERSION = OSSA_VERSION;

// Schema URL
export const OSSA_SCHEMA_URL = `https://openstandardagents.org/schema/v${OSSA_VERSION}/ossa-${OSSA_VERSION}.schema.json`;

// Display version for bot messages
export const OSSA_DISPLAY_VERSION = "0.2.x";
