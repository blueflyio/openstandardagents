openapi: 3.1.0
info:
  title: openapi-3-1-generator Agent API
  version: 1.0.0
  description: OSSA-compliant API specification for openapi-3-1-generator
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Agent is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: healthy
                  agent:
                    type: string
                    example: openapi-3-1-generator
                  version:
                    type: string
                    example: 1.0.0
