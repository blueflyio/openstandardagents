openapi: 3.1.0
info:
  title: opa-policy-architect Agent API
  version: 1.0.0
  description: OSSA-compliant API specification for opa-policy-architect
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Agent is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: healthy
                  agent:
                    type: string
                    example: opa-policy-architect
                  version:
                    type: string
                    example: 1.0.0
