# GitLab CI/CD for daily-code-scan
# Auto-generated from OSSA manifest

stages:
  - build
  - deploy

build:
  stage: build
  image: node:20-alpine
  script:
    - npm ci
    - npm run build
    - npm test
  artifacts:
    paths:
      - dist/
      - node_modules/
    expire_in: 1 hour

deploy:daily-code-scan:
  stage: deploy
  image: docker:24
  services:
    - docker:24-dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t registry.gitlab.com/$CI_PROJECT_PATH/daily-code-scan:latest .
    - docker push registry.gitlab.com/$CI_PROJECT_PATH/daily-code-scan:latest
    # Deploy to Kubernetes
    - kubectl set image deployment/daily-code-scan daily-code-scan=registry.gitlab.com/$CI_PROJECT_PATH/daily-code-scan:latest -n agents
  only:
    - main
