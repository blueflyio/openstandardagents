# Changelog

All notable changes to OSSA will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Add 'Treating Agents as First-Class Citizens' tagline to homepage
- Implement template system for dynamic versioning
- Automatically fetch and display latest dev and stable tags from GitHub
- Major overhaul with real-time validation and improved UX
- Add review apps for OrbStack K8s

### Documentation

- Add Symphony + Drupal integration research and planning

### Fixed

- Use Job-Token header for CI_JOB_TOKEN auth
- Mobile docs sidebar responsiveness
- Use CI_JOB_TOKEN for Docker registry authentication
- Merge main into release/v0.3.x - resolve conflicts
- Preserve examples when GitLab API fails
- Preserve examples when GitLab API fails
- Use nip.io for review apps DNS - no /etc/hosts needed
- Use KUBECONFIG_LOCAL to match existing naming convention
- Use blueflyagents.com domain for review apps
- Docker auth + artifact path for build:docker
- Inline detect-version + use needs for DAG
- Add missing detect-version.sh script
- Use feature branches + MRs instead of direct push to protected branches
- Use local runners and disable security scans

### Hotfix

- Add CI_JOB_TOKEN for private repo access

### Release

- V0.2.9 website update

<!-- generated by git-cliff -->
