#!/usr/bin/env tsx

// OSSA CLI v0.1.3 with UADP Discovery Protocol
// Enhanced agent management with discovery capabilities

import('../src/index.js').catch((error) => {
  console.error('Failed to load OSSA CLI:', error.message);
  process.exit(1);
});