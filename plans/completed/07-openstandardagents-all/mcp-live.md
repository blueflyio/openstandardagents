# Task: MCP live

- Ensure MCP (agent-protocol) is running and reachable at https://mcp.blueflyagents.com (or Tailscale backup).
- Verify tools (GitLab, skills, etc.) and health endpoint.
- Deploy if needed: `buildkit deploy oracle mcp`.
