# Task: Router live

- Ensure agent-router is running and reachable at https://router.blueflyagents.com (or backup).
- Verify LLM routing and health; deploy if needed: `buildkit deploy oracle router`.
