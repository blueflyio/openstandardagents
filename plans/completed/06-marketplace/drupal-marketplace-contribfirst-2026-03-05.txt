HANDOFF NOTE — 2026-03-05
Site: WORKING_DEMOs/Drupal_AgentMarketplace
URL: https://agent-marketplace.ddev.site:8443

ACTIVE AGENTS (check if still running)
========================================
6 agents fixing 744 bracket-corrupted PHP files.
Verify: find web/modules/custom/ -name "*.php" -exec php -l {} \; 2>&1 | grep "Errors parsing" | wc -l

fix-mcp (267), fix-orchestra (178), fix-recipe (112), fix-hf (75), fix-layout (84), fix-misc (35)

COMPLETED THIS SESSION
========================================
- 101 files: hallucinated Drupal\llm imports, wrong base classes, old annotations
- info.yml dep cleanup (9 modules), composer alias fixes
- Added drupal/dynamic_entity_reference, views_bulk_operations, webform
- drush unblocked (7 mcp_registry entity files)

STRATEGIC CONTRIB-FIRST REALIGNMENT (7 PILLARS)
========================================

Pillar 1: Tool API (drupal/tool + tool_belt)
  - All executable logic as standard Tool plugins
  - Delete custom registries, JSON parsing, proprietary formats

Pillar 2: State/Workflows (drupal/eca + maestro + orchestration)
  - Replace ServiceIntegrationOrchestrator, FleetOrchestratorService
  - ECA internal, Maestro visual state machines, Orchestration external bridges

Pillar 3: AI Discovery (drupal/ai + ai_agents + ai_agents_ossa)
  - All LLM routing via drupal/ai, delete custom provider bridges
  - ai_agents for capability mapping, ai_agents_ossa for manifest parsing

Pillar 4: API Normalization (drupal/api_normalization)
  - Auto-generate Tool plugins from OpenAPI specs
  - Ban raw @http_client in custom modules

Pillar 5: Visual Workflows (drupal/flowdrop ecosystem)
  - flowdrop + flowdrop_ui + flowdrop_tool_provider + flowdrop_ai

Pillar 6: Observability (drupal/audit_export + OpenTelemetry)
  - Drop custom metric tables, pipe to tracer.blueflyagents.com

Pillar 7: Config/Admin UIs (drupal/key, config/schema, admin forms)
  - Zero hardcoded endpoints, everything via admin UI

MODULE DISPOSITION
========================================
DELETE (replace with contrib):
  mcp_registry (322) -> drupal/mcp + mcp_server
  ai_agents_orchestra (275) -> orchestration + eca + maestro
  ai_agents_crewai (83) -> orchestration
  ai_agents_huggingface (124) -> drupal/ai HF provider
  ai_agents_dashboard (40) -> drupal/ai admin UI
  layout_system_converter (212) -> drupal/canvas 1.1
  recipe_onboarding (213) -> DrupalCMS 2.0 recipe mgmt

KEEP + REFACTOR: cedar_policy, ai_agents_client, ai_agents_communication,
  ai_agents_tunnel, ai_provider_apple, alternative_services, api_normalization,
  code_executor, dita_ccms, dragonfly_client, blockchain_manager,
  drupal_patch_framework, + small modules

NEXT SESSION
========================================
1. Verify bracket fixes complete (php -l count = 0)
2. Enable remaining modules
3. Begin module deletions (mcp_registry first)
4. Start Pillar 1: Tool API migration
5. Build Playwright E2E tests + Dragonfly
