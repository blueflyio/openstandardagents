# Task: GitLab CI, MR, runners

- Verify GitLab CI/CD group variables (GITLAB_COMPONENTS_VERSION, DRUPAL_SIMPLE_VERSION, etc.).
- Check MR list and pipeline status; merge when CI passes.
- Confirm runners (NAS/Oracle only; never mac-m3/mac-m4 for platform). Use `buildkit gitlab runners` / vars as needed.
