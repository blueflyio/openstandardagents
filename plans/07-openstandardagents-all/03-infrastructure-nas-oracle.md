# Task: Infrastructure NAS / Oracle

- NAS: config master, code-server, wikis; path Mac `/Volumes/AgentPlatform/`, NAS SSH `/volume1/AgentPlatform/`.
- Oracle: production services (MCP, mesh, GKG, router, tracer, etc.); deploy with `buildkit deploy oracle <service>`.
- Ensure tunnel and service routes match AGENTS.md published routes table.
