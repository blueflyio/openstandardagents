/**
 * OpenAI Agents SDK Adapter
 *
 * Exports OSSA manifests to runnable @openai/agents TypeScript packages.
 * Makes OSSA the universal agent contract — define once, run on OpenAI.
 */

export { OpenAIAgentsAdapter } from './adapter.js';
import { OpenAIAgentsAdapter } from './adapter.js';
export default OpenAIAgentsAdapter;
