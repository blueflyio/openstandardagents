{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openstandardagents.org/spec/uadp/v0.1/schemas/uadp-agents-response.schema.json",
  "title": "UADP Agents Response",
  "description": "Schema for GET /uadp/v1/agents response.",
  "type": "object",
  "required": ["data", "meta"],
  "properties": {
    "data": {
      "type": "array",
      "items": { "$ref": "#/$defs/OssaAgent" }
    },
    "meta": { "$ref": "#/$defs/PaginationMeta" }
  },
  "additionalProperties": false,
  "$defs": {
    "OssaAgent": {
      "type": "object",
      "required": ["apiVersion", "kind", "metadata"],
      "properties": {
        "apiVersion": {
          "type": "string",
          "pattern": "^ossa/v\\d+\\.\\d+$"
        },
        "kind": {
          "type": "string",
          "const": "Agent"
        },
        "metadata": {
          "type": "object",
          "required": ["name"],
          "properties": {
            "name": { "type": "string", "minLength": 1 },
            "version": { "type": "string" },
            "description": { "type": "string" },
            "uri": {
              "type": "string",
              "pattern": "^uadp://"
            },
            "trust_tier": {
              "type": "string",
              "enum": ["official", "verified-signature", "signed", "community", "experimental"]
            },
            "created": { "type": "string", "format": "date-time" },
            "updated": { "type": "string", "format": "date-time" }
          },
          "additionalProperties": true
        },
        "spec": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },
    "PaginationMeta": {
      "type": "object",
      "required": ["total", "page", "limit", "node_name"],
      "properties": {
        "total": { "type": "integer", "minimum": 0 },
        "page": { "type": "integer", "minimum": 1 },
        "limit": { "type": "integer", "minimum": 1, "maximum": 100 },
        "node_name": { "type": "string" }
      },
      "additionalProperties": true
    }
  }
}
