name: 'OSSA MCP Bridge'
type: module
description: 'Bridge module for OSSA (Open Standards Scalable Agents) MCP protocol integration'
core_version_requirement: ^9 || ^10 || ^11
package: 'OSSA'
version: '0.1.9-alpha.1'

dependencies:
  - drupal:system
  - drupal:user

configure: ossa_mcp_bridge.settings

libraries:
  - ossa_mcp_bridge/admin

test_dependencies:
  - drupal:phpunit_bridge
