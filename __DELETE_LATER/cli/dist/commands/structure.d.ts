#!/usr/bin/env node
/**
 * OSSA CLI Project Structure Commands
 * Implements the 3-level deep project structure standard
 */
import { Command } from 'commander';
export declare function createStructureCommands(): Command;
